Task 10 cross-store evidence

Command: cargo test --lib config::outputs::resolver
Result: 12 resolver tests passed.

Covered by test: config::outputs::resolver::tests::cross_store_ref_extracts_store_slug_and_secret_path
Case: github:org/secrets#prod/api-key resolves to:
- store_slug = Some("org/secrets")
- secret_path = prod/api-key

Implementation delegates store parsing through SecretRef::parse_store_ref and full reference parsing through SecretRef::parse.
